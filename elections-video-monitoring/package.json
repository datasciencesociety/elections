{
  "name": "bg-election-dashboard",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter coordinator dev",
    "start": "pnpm --filter coordinator start",
    "test": "pnpm --filter coordinator test",
    "scrape": "pnpm --filter scraper scrape",
    "seed": "node scripts/seed.js"
  },
"devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
