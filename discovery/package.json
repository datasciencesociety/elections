{
  "name": "discovery",
  "private": true,
  "type": "module",
  "description": "Service discovery + metrics aggregator for Hetzner analyzer boxes. Read-heavy; pre-gzipped in-memory blob serves /video/metrics.",
  "scripts": {
    "dev": "npx tsx watch src/index.ts",
    "start": "npx tsx src/index.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "better-sqlite3": "^11.7.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "tsx": "^4.19.0"
  }
}
